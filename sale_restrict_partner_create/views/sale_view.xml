<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_restrict_create_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.restrict.create</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <!-- Restrict quick create on Customers (partner_id) -->
            <xpath expr="//field[@name='partner_id']" position="attributes">
                <attribute name="options">{'no_create': True, 'no_create_edit': True, 'always_reload': True}</attribute>
            </xpath>

            <!-- Restrict quick create on Invoice Address (partner_invoice_id) if visible -->
            <xpath expr="//field[@name='partner_invoice_id']" position="attributes">
                <attribute name="options">{'no_create': True, 'no_create_edit': True, 'always_reload': True}</attribute>
            </xpath>

            <!-- Restrict quick create on Delivery Address (partner_shipping_id) if visible -->
            <xpath expr="//field[@name='partner_shipping_id']" position="attributes">
                <attribute name="options">{'no_create': True, 'no_create_edit': True, 'always_reload': True}</attribute>
            </xpath>
        </field>
    </record>
</odoo>
